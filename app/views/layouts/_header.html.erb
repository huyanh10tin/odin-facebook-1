<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Longhouse', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Welcome, <%= current_user.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to 'Friends', user_friends_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to user_friend_requests_path(current_user) do %>
                  Friend requests
                  <span class="badge"><%= current_user.num_of_friend_requests %></span>
                <% end %>
              </li>
            </ul>
          </li>
          <li><%= link_to 'All users', users_path %></li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete do %>
              Sign out
              <span class="glyphicon glyphicon-log-out"></span>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to new_user_session_path do %>
              Sign in
              <span class="glyphicon glyphicon-log-in"></span>
            <% end %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
